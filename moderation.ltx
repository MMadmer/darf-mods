; Reviews the catalog does not count (MOD_CATALOG 12.4), applied by watch.yml. The raw inbox
; stays public: anyone can recount it.

; a reviewer identity (key id) whose reviews are all removed
[identities]

; a hardware hash (64 hex) whose reviews are all removed
[hardware]

; one review: "<module id> <identity key id>"
[reviews]
