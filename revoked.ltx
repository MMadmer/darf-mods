; Revoked modules, versions and author keys (MOD_CATALOG 5.2). catalog.py revoke adds a line,
; catalog.py publish signs them into public/index.ltx. The game refuses a revoked release and
; switches off an installed one at its next start.
;
; <module id>         = <version or *>, <reason>
; key:<author key id> = *, <reason>
[revoked]
